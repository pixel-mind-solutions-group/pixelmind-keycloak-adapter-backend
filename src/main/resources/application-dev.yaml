
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/keycloak26
    username: keycloak26
    password: PixelmindSriLanka
    driverClassName: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none